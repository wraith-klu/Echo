[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests as requiring real AI models (deselect with -m 'not integration')
    slow: marks tests as slow-running
addopts = -v --tb=short --durations=10
